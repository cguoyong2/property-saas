# 交付前代码审查报告

## 审查范围

按 `prompts/Codex代码审查提示词模板.md` 对最终工程包做只读交付审查，重点检查多租户、权限、金额、支付幂等、OpenAPI 权限、mock 数据、高风险操作日志和前端上下文。

## 结论

未发现阻止工程包交付验收的 P0 代码问题。后端、PC、小程序均已从最终 zip 的干净副本构建通过，联调烟测通过。

TASK-028 已补充生产包干净环境部署演练，从 zip 干净副本验证生产环境变量检查、生产 profile 空库迁移到 v19、后端测试打包、PC 管理端构建和 uni-app H5/微信小程序构建。演练中发现并修复了非 Web 启动下 `OperationLogService` 请求对象注入和 `SecurityConfig` 安全链创建问题。

TASK-029 已完成移动端依赖安全治理，生产依赖 audit 已降至 `0 high`、`0 critical`，H5 和微信小程序构建继续通过。

TASK-030 已完成生产上线资料闭环与部署预案，补齐 Go/No-Go 门禁、资料缺口台账、部署步骤、冒烟验证和代码/数据库/配置回滚方案。

TASK-031 已完成生产迁移 SQL 兼容性清理，生产迁移和生产初始化脚本不再使用 MySQL 8.0 已提示弃用的 `VALUES()` 重复键更新写法。

交付前仍有以下需要项目负责人评估的生产化问题。

## 问题清单

### 已整改 高风险操作统一审计日志

- 文件位置：
  - `backend/src/main/java/com/yongquan/propertysaas/system/audit/service/OperationLogService.java`
  - `backend/src/main/java/com/yongquan/propertysaas/system/audit/controller/OperationLogController.java`
  - `admin-web/src/config/pages.ts`
- 当前状态：已新增显式审计服务、查询接口和 PC 端 `/platform/audit` 页面，覆盖账单作废、退款、授权、合同、工单派单和设备同步等高风险动作。
- 剩余建议：后续可按客户审计制度继续扩展普通 CRUD 审计范围。

### P1 真实微信支付能力仍需接入真实商户资料

- 文件位置：
  - `backend/src/main/java/com/yongquan/propertysaas/payment/wechat/WechatPayClientImpl.java`
  - `backend/src/main/resources/application-prod.yml`
- 问题说明：已拆分 `DEV_SIMULATED` 和 `REAL_WECHAT_V3`。生产模式不再继续使用开发 HMAC，但真实微信支付 v3 服务商能力仍依赖外部商户号、证书、API v3 key 和回调公网地址。
- 修改建议：取得真实商户资料后，按接入方案实现 HTTP 原始报文验签、通知报文解密、时间戳/nonce 重放防护、证书轮换、失败告警和回调重试验收。

### P1 生产环境必须使用生产 profile 和生产初始化

- 文件位置：
  - `backend/src/main/resources/application-prod.yml`
  - `backend/src/main/resources/db/migration-prod`
  - `sql/seed_prod_minimal.sql`
  - `scripts/prod-env-check.sh`
- 问题说明：已拆分生产 profile、生产迁移和生产最小初始化模板；本地开发配置仍保留演示默认值。
- 修改建议：生产部署必须使用 `SPRING_PROFILES_ACTIVE=prod`、`migration-prod`、`seed_prod_minimal.sql` 和真实环境变量，并在上线前执行 `scripts/prod-env-check.sh`。

### P2 前端 token 持久化需要配套 XSS 防护

- 文件位置：
  - `admin-web/src/store/auth.ts:16`
  - `mobile-uniapp/src/store/member.ts:22`
- 问题说明：PC 管理端和小程序端均将 token 写入本地持久化存储。当前符合演示和开发便利性，但生产环境需要控制 token 泄露影响。
- 修改建议：补充 CSP、输入输出转义、安全 Header、短 TTL、刷新机制、退出登录全量清理和异常登录告警。

### 已整改 PC 管理端生产构建性能

- 文件位置：
  - `admin-web/src/router/index.ts`
  - `admin-web/src/config/pages.ts`
- 当前状态：已完成路由动态导入、Vite `manualChunks` 和 Element Plus 按需注册。最大 JS chunk 已降至 `365.75 kB`，构建不再出现 500 kB chunk 警告。
- 剩余建议：如继续优化首屏，可评估 Element Plus 样式按需加载。

### 已整改 生产包干净环境部署演练

- 文件位置：
  - `tasks/TASK-028_生产包干净环境部署演练.md`
  - `docs/reports/TASK-028_生产包干净环境部署演练报告.md`
  - `backend/src/main/java/com/yongquan/propertysaas/system/audit/service/OperationLogService.java`
  - `backend/src/main/java/com/yongquan/propertysaas/security/config/SecurityConfig.java`
- 当前状态：已从 zip 干净副本完成生产环境变量、生产迁移、后端测试打包、PC 构建和移动端构建验证。
- 剩余建议：MySQL 8.0 `VALUES()` 弃用警告已在 TASK-031 清理；后续按 TASK-026 继续治理移动端依赖漏洞。

### 已整改 生产迁移 SQL 兼容性警告

- 文件位置：
  - `backend/src/main/resources/db/migration`
  - `backend/src/main/resources/db/migration-prod`
  - `sql/seed_prod_minimal.sql`
  - `tasks/TASK-031_生产迁移SQL兼容性清理.md`
  - `docs/reports/TASK-031_生产迁移SQL兼容性清理报告.md`
- 当前状态：已将 `ON DUPLICATE KEY UPDATE` 中的 `VALUES(col)` 改为 `AS new` 插入别名和 `new.col` 引用；生产临时空库迁移到 v19，迁移日志未出现弃用警告。
- 剩余建议：真实上线前在预生产 MySQL 实例上复跑生产迁移并归档完整日志。

### 已整改 移动端依赖高危风险

- 文件位置：
  - `mobile-uniapp/package.json`
  - `mobile-uniapp/package-lock.json`
  - `tasks/TASK-029_移动端依赖安全治理与构建升级评估.md`
  - `docs/reports/TASK-029_移动端依赖安全治理与构建升级评估报告.md`
- 当前状态：已升级 DCloud Vue3 工具链并通过 npm overrides 降低 `@intlify/*`、`esbuild`、`vite` 相关风险。生产依赖 audit 已无 high/critical。
- 剩余建议：全量 audit 仍有 DCloud 开发服务链路高危，生产部署不得暴露 Vite/uni 开发服务，等待 DCloud 官方后续安全版本继续治理。

### 已整改 生产上线资料闭环

- 文件位置：
  - `docs/deploy/生产上线资料收集表.md`
  - `docs/deploy/生产部署预案与回滚方案.md`
  - `tasks/TASK-030_生产上线资料闭环与部署预案.md`
  - `docs/reports/TASK-030_生产上线资料闭环与部署预案报告.md`
- 当前状态：已补齐生产上线 Go/No-Go 门禁、资料缺口台账、部署步骤、冒烟验证和回滚方案。
- 剩余建议：真实上线前由项目负责人、运维、支付负责人、DBA、小程序管理员和第三方厂商补齐资料并签核。

## 正向检查项

- 业务表未发现遗漏 `tenant_id` 的问题，平台全局表除外。
- 控制器业务接口均有认证或权限保护，登录、健康检查、支付回调为明确公开入口。
- 小程序房屋绑定已加固为会员登录且 `tenantId/memberId` 必须与 JWT 上下文一致。
- 支付回调已从订单记录反查 `tenant_id`，不再依赖订单号格式。
- 未发现接口返回 mock 数据后标记完成的代码。
- 金额字段使用 Java `BigDecimal` 和 MySQL `DECIMAL`。
- 联调脚本已覆盖跨租户访问拒绝和支付错误签名拒绝。
