# 当前开发缺口复核与收尾验证报告

## 1. 复核范围

- 对照 `tasks/开发任务总清单.md`、`docs/reports/最终交付索引与移交说明.md`、`docs/reports/上线前风险清单.md`、`docs/deploy/生产上线资料收集表.md` 和 `docs/deploy/预生产演练执行清单.md`。
- 复核口径为“当前可开发范围内的代码功能”，不把项目方已确认后置的真实微信支付、生产资料和第三方厂商实机对接计入当前开发缺口。

## 2. 复核结论

- 正式定义的主线开发任务已到 `TASK-048`，当前没有新的正式任务文件。
- 当前代码工作区已继续完成 TASK-046 至 TASK-048 后的现场业务收尾，覆盖基础档案顺序、楼栋/单元/房屋联动、业主/住户维护、车位车辆、收费账务、部分收款、预存款、退款和房屋绑定审核等细节。
- 当前开发迁移与生产迁移最新文件均为 `V43__member_prepayment_refund_amount.sql`。
- 仍需在正式上线前完成生产资料、预生产演练、备份恢复演练、Go/No-Go 签核，以及项目方确认后置的微信支付和第三方厂商实机联调。

## 3. 本次收尾补充

- 扩展 `scripts/integration-business-actions.sh`：
  - 业主/住户按关键字自动检索；
  - 检查检索结果包含小区名称和房号核对字段；
  - 检查房屋绑定审核、车位区域、车位、车辆、车辆品牌、车辆型号列表；
  - 检查账单自动计算结果、账单详情、业主/住户、房号和应收明细字段；
  - 检查收款订单列表和退款可选已收款账单；
  - 增加 `RUN_MUTATING_ACCOUNTING_CHECKS=true` 开关，显式开启后才执行会改变账务金额的现金部分收款回归。
- 修复干净环境启动与收尾验证发现的问题：
  - 关闭 Flyway placeholder 替换，避免消息模板中的 `${变量}` 被误识别为迁移占位符；
  - 将 V29、V30、V32 迁移调整为可在 MySQL 8.0 空库中稳定执行；
  - 补齐业主/住户查询返回的小区名称、楼栋、单元和房号核对字段；
  - 修复业务集成脚本中文查询参数编码，保证自动检索用例可重复执行。

## 4. 最新验证记录

验证时间：2026-06-27。

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 空库迁移启动 | `mvn spring-boot:run ... --server.port=19150 --spring.datasource.url=jdbc:mysql://localhost:3310/property_saas...` | 通过，Flyway 成功应用 43 个迁移到 v43 |
| 基础联调烟测 | `API_BASE_URL=http://localhost:19150 ./scripts/integration-smoke.sh` | 通过 |
| 业务动作集成验证 | `RUN_MUTATING_ACCOUNTING_CHECKS=true API_BASE_URL=http://localhost:19150 ./scripts/integration-business-actions.sh` | 通过 |
| 后端单元测试 | `mvn test` | 通过，8 个测试全部通过 |

## 5. 后置事项

| 事项 | 状态 | 说明 |
| --- | --- | --- |
| 真实微信支付 v3 | 后置 | 等真实服务商、子商户、证书、API v3 key 和回调地址到位后联调 |
| 生产资料 | 后置 | 等正式部署环境资料到位后填写并执行 `scripts/prod-env-check.sh` |
| 第三方厂商设备 | 后置 | 等门禁、停车、梯控、短信厂商资料和设备安装后联调 |
| 预生产演练 | 待执行 | 需在预生产或空生产环境按 `docs/deploy/预生产演练执行清单.md` 记录执行 |
| 云服务器部署 | 待执行 | 当前需要可用 SSH 登录方式后再发布最新构建 |
