# 上线前风险清单

## P0 必须上线前关闭

| 编号 | 风险 | 证据 | 建议 |
|---|---|---|---|
| R001 | 真实微信支付 v3 原始报文验签、resource 解密和重放防护仍需商户资料到位后完成。 | `backend/src/main/java/com/yongquan/propertysaas/payment/wechat/WechatPayClientImpl.java`、`docs/reports/TASK-024_微信支付v3服务商接入骨架报告.md` | 已拆分 `DEV_SIMULATED` 与 `REAL_WECHAT_V3`，生产模式不再走本地 HMAC；项目方已确认真实微信支付对接后置到正式部署后处理。 |
| R002 | 演示支付密钥和演示账号只适合本地开发。 | `sql/seed_demo_data.sql`、`sql/seed_prod_minimal.sql`、`backend/src/main/resources/db/migration-prod`、`mobile-uniapp/src/pages/login/index.vue` | 已拆分演示与生产初始化；小程序登录演示默认值已限制为开发模式；生产必须使用 `migration-prod` 和 `seed_prod_minimal.sql`，不得执行演示种子数据。 |
| R003 | 默认数据库密码、MinIO 密码、JWT 本地密钥存在于开发配置。 | `.env.example`、`.env.prod.example`、`backend/src/main/resources/application-prod.yml`、`scripts/prod-env-check.sh` | 已提供生产 profile 和环境变量检查；上线前必须填真实密钥并通过 `scripts/prod-env-check.sh`。 |

## P1 上线前应关闭

| 编号 | 风险 | 证据 | 建议 |
|---|---|---|---|
| R004 | 高风险业务操作需要持续扩展审计覆盖面。 | `docs/reports/TASK-023_高风险操作审计日志报告.md` | 已接入显式审计服务并覆盖账单作废、退款、授权、合同、工单、设备等关键动作；后续按客户要求扩展普通 CRUD。 |
| R005 | 第三方门禁、停车、梯控仍为适配层和演示同步逻辑，没有真实厂商验收。 | `docs/deploy/第三方设备厂商接入验收模板.md` | 已补充厂商接入验收模板；项目方已确认第三方厂商对接后置到设备安装后处理。 |
| R006 | `mobile-uniapp` 依赖链存在 npm audit 风险。 | `docs/reports/TASK-026_依赖安全与生产构建优化评估.md`、`docs/reports/TASK-029_移动端依赖安全治理与构建升级评估报告.md` | 已升级 DCloud Vue3 工具链并通过 overrides 降低高危风险；生产依赖 audit 已降至 0 high/0 critical。全量 audit 仍有 DCloud 开发服务链路高危，生产不得暴露 Vite/uni 开发服务。 |
| R007 | PC 管理端构建产物单 JS chunk 曾超过 1 MB。 | `docs/reports/TASK-027_PC管理端生产构建性能优化报告.md` | 已通过路由懒加载、Element Plus 按需注册和 `manualChunks` 关闭；最大 JS chunk 降至 `365.75 kB`，当前为已整改项。 |
| R011 | 生产迁移 SQL 曾触发 MySQL 8.0 `VALUES()` 弃用警告。 | `docs/reports/TASK-031_生产迁移SQL兼容性清理报告.md` | 已改为 `AS new` 插入别名和 `new.col` 引用，生产临时空库迁移到 v19 且日志无弃用警告；真实上线前仍需在预生产 MySQL 实例复跑迁移。 |

## P2 可作为上线后优化

| 编号 | 风险 | 证据 | 建议 |
|---|---|---|---|
| R008 | PC 端 JWT 存储在 `localStorage`，小程序 token 存储在本地 storage。 | `admin-web/src/store/auth.ts:16`、`mobile-uniapp/src/store/member.ts:22`、`docs/deploy/nginx-security-headers.conf` | 已补充 PC 管理端 Nginx 安全响应头模板；生产仍建议配合短 token TTL、刷新令牌或服务端会话策略降低泄露影响。 |
| R009 | PC 登录页默认填入演示账号密码，适合开发但不适合生产。 | `admin-web/src/views/auth/LoginView.vue:25` | 生产构建移除默认账号，或通过环境变量控制 demo 填充。 |
| R010 | Docker Compose 使用固定 `container_name` 和固定 MinIO 端口，容易与本机已有服务冲突。 | `docker-compose.yml:4`、`docker-compose.yml:33`、`docker-compose.yml:38` | 生产部署使用明确项目名、环境变量端口和独立 compose profile；本机已有 9000/9001 服务时需调整 MinIO 端口。 |

## 当前验收环境观察

- MySQL/Redis 使用当前已有 `property-saas-mysql`、`property-saas-redis` 容器验证通过。
- 本机 9000/9001 已被其他 MinIO 服务占用，未启动工程包 MinIO 容器；当前后端烟测未依赖 MinIO。
- 小程序依赖安装、H5 构建和微信小程序构建可通过；生产依赖 audit 已无 high/critical，仍需等待 DCloud 后续版本继续降低中低风险。
- TASK-032 已完成上线前最终清单复核；工程包内部资料可进入项目方上线准备，真实生产上线仍需完成外部资料、预生产迁移、支付联调、备份恢复演练和 Go/No-Go 签核。
- TASK-033 已从最新完成版 zip 解压干净副本复验后端、PC、移动端构建和生产迁移；工程包内部交付链路可复现。
- TASK-035 已按项目方确认将真实微信支付、生产资料完善和第三方厂商对接后置，并完成小程序演示默认值生产隔离和 PC 安全响应头模板。
