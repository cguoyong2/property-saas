# TASK-035 后置项确认与前端生产安全收尾

## 目标

根据项目方确认，将真实微信支付、生产资料填报和第三方厂商对接后置到正式部署或设备安装阶段；本任务完成当前工程包内仍可处理的生产安全收尾项。

## 已完成内容

1. 小程序登录页演示默认值改为仅开发模式预填，生产构建不再预置 `tenantId`、`openid`、手机号和姓名。
2. 新增 `docs/deploy/nginx-security-headers.conf`，提供 PC 管理端静态资源生产部署安全响应头模板。
3. 更新生产部署预案，明确 PC 管理端发布时应套用安全 Header 模板。
4. 更新上线前风险清单、最终交付索引和 README，将以下事项标记为项目方确认后置：
   - 真实微信支付 v3 对接：正式部署后再接入。
   - 生产资料完善：正式部署时补齐。
   - 第三方厂商对接：设备安装后再对接。
5. 更新任务总清单、发行说明和 Manifest。

## 验收结果

| 检查项 | 结果 |
| --- | --- |
| 小程序生产默认登录值 | 已移除，仅 `import.meta.env.DEV` 下预填 |
| PC 安全 Header 模板 | 已新增 |
| 后置项文档口径 | 已同步 |
| 移动端类型检查与双端构建 | 通过 |
| PC 构建 | 通过 |

执行命令：

```bash
rg -n "demo-openid|tenantId: '1'|nginx-security-headers|后置" README.md docs tasks mobile-uniapp/src
npm run build
npx vue-tsc --noEmit
npm run build:h5
npm run build:mp-weixin
```

结果：通过。

## 后置事项

以下事项不是当前工程包内部阻塞项，按项目方确认后置：

1. 微信支付真实接入：正式部署后接入真实商户资料、证书、API v3 key、回调解密和防重放。
2. 生产资料完善：正式部署时填写域名、证书、数据库、Redis、对象存储、首个平台管理员和 Go/No-Go 签核。
3. 第三方厂商对接：门禁、停车、梯控、短信等设备安装后按厂商资料联调。

## 未完成事项

无新的工程包内部阻塞项。后续建议进入真实部署准备，而不是继续追加工程包内收口任务。
