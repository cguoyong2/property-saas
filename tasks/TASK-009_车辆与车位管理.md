# TASK-009 车辆与车位管理

## 任务目标

实现车辆、车位、月租状态和停车系统映射预留。

## 输入文档

- `AGENTS.md`
- `README.md`
- `docs/00-CODEX使用说明.md`
- `docs/architecture/02-技术架构说明.md`
- `docs/architecture/03-多租户设计.md`
- `sql/schema_mysql8.sql`
- `openapi/openapi.yaml`
- `tests/验收测试用例.md`

## 开发边界

本任务只完成 `车辆与车位管理` 相关内容。不要顺手开发后续模块，避免产生未经确认的接口、字段和状态。

## 禁止事项

- 不允许绕过租户隔离。
- 不允许临时伪造接口返回。
- 不允许修改已冻结枚举和接口路径。
- 不允许把多个业务职责混在一个 Service 中。


## 通用完成标准

- 与 `AGENTS.md` 不冲突。
- 与 `sql/schema_mysql8.sql` 不冲突。
- 与 `openapi/openapi.yaml` 不冲突。
- 所有业务接口必须校验权限。
- 所有业务数据必须校验租户隔离。
- 必须补充必要的单元测试或接口测试说明。
- 不得使用 mock 数据冒充完成。

## 交付输出

1. 修改/新增文件清单；
2. 实现说明；
3. 数据库变更说明；
4. 接口变更说明；
5. 测试命令和测试结果；
6. 未完成事项和风险。

