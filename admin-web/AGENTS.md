# admin-web/AGENTS.md

## 管理端开发约束

1. 使用 Vue 3 + TypeScript + Element Plus。
2. 菜单必须由后端 `/api/auth/menus` 返回，不允许前端硬编码权限。
3. 页面按钮必须按后端返回的按钮权限控制，同时后端接口也必须校验权限。
4. 所有列表页必须包含租户/项目数据范围下的筛选、分页、排序。
5. 涉及账务、退款、作废、导出、批量导入的页面必须增加二次确认和原因填写。
6. 平台运营端与物业租户端页面必须在路由、菜单、权限上分离。
